VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-side only (scripts/seedTestData.ts). Never prefix with VITE_.
SUPABASE_SERVICE_ROLE_KEY=

# Web push (VAPID). Generate with:
#   node -e "const{generateKeyPairSync}=require('crypto');const{publicKey,privateKey}=generateKeyPairSync('ec',{namedCurve:'prime256v1'});const p=publicKey.export({format:'jwk'});console.log(Buffer.concat([Buffer.from([4]),Buffer.from(p.x,'base64url'),Buffer.from(p.y,'base64url')]).toString('base64url'),Buffer.from(privateKey.export({format:'jwk'}).d,'base64url').toString('base64url'))"
VITE_VAPID_PUBLIC_KEY=

# Server-side only (edge function). Never prefix with VITE_.
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=
